export interface StartingEquipment {
    count: number,
    results: {
        class: string,
        url: string
    }
};

export interface StartingEquipmentDetails {
    id: string,
    index: number,
    starting_equipment: {
        
    }
}